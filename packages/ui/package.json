{
  "name": "@munch/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared design tokens for Munch: the platform-agnostic visual language (colors, typography, spacing, radii, shadows). No React Native / no DOM imports.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings 0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
