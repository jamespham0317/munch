/// <reference types="expo/types" />

// Ambient types for the Expo runtime and EXPO_PUBLIC_* env vars. Normally Expo
// regenerates this; it is committed so `tsc` typechecks in CI without a prior
// `expo` CLI run. Do not add app code here.
